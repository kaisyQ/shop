import styled from "styled-components";


export const ImageWrapper = styled.img`
    width: 100%;
    transform: scale(100%);
    transition: .3s ease-in-out;
    cursor: pointer;
    :hover {
        transform: scale(120%);
        transition: .3s ease-in-out;
    }

`;