import styled from 'styled-components';

export const Wrapper = styled.div`
    position: absolute;
    right: 2rem;
    top: 1rem;
    display: flex;
    align-items: center;
    gap: 1rem;
`;

export const Text = styled.p`
    font-weight: 600;
    color: darkred;
`;
