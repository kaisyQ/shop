import styled from "styled-components";

const FormWrapper = styled.form`
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    max-width: 330px;
    gap: 2rem;
    width: 100%;
`;


const FormElement = styled.div`
`;

export default {
    FormElement, FormWrapper
}