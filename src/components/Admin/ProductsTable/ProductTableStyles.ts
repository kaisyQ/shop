import styled from "styled-components";

export const TopCheckboxLabel = styled.label`
    display: flex;
    gap: 1rem;
    align-items: center;
    font-size: 1.6rem;
`;

export const Checkbox = styled.input`
    width: 2rem;
    height: 2rem;
`;