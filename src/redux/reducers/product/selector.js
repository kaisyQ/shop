export const getProductItems = (state) => state.product.items

export const getCurrentProduct = (state) => state.product.current

export const getCartItems = (state) => state.product.cartItems

export const getCartItemCount = (state) => state.product.cartItems.length

export const getTotalPrice = (state) => state.product.totalPrice
